@model bcpp.Models.akcie

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>akcie</legend>

        @Html.HiddenFor(model => model.akcie_id)

        <div class="editor-label">
            @Html.LabelFor(model => model.firma_id, "firma")
        </div>
        <div class="editor-field">
            @Html.DropDownList("firma_id", String.Empty)
            @Html.ValidationMessageFor(model => model.firma_id)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.nazev)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.nazev)
            @Html.ValidationMessageFor(model => model.nazev)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.zkratka)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.zkratka)
            @Html.ValidationMessageFor(model => model.zkratka)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

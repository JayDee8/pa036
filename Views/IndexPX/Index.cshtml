@model IEnumerable<bcpp.Models.index_PX>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.hodnota)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.hodnota)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.datum }) |
            @Html.ActionLink("Details", "Details", new { id=item.datum }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.datum })
        </td>
    </tr>
}

</table>

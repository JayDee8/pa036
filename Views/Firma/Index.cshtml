@model IEnumerable<bcpp.Models.firma>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.adresa.mesto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nazev)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ICO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DIC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.adresa.mesto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nazev)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ICO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DIC)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.firma_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.firma_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.firma_id })
        </td>
    </tr>
}

</table>
